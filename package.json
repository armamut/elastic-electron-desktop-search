{
  "name": "desktop-search-electron-elasticsearch",
  "version": "1.0.0",
  "description": "Electron desktop search application",
  "main": "app/main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-packager . $npm_package_productName --out=dist --ignore='^/dist$' --prune --asar --all --version=$npm_package_electronVersion"
  },
  "repository": "",
  "keywords": [
    "Electron",
    "elasticsearch",
    "desktop search",
    "file search",
    "desktop",
    "search",
    "tutorial",
    "indexing"
  ],
  "author": "armamut",
  "license": "MIT",
  "devDependencies": {
    "electron": "^1.4.1",
    "electron-packager": "^8.5.2"
  },
  "dependencies": {
    "async": "^2.1.5",
    "elasticsearch": "^12.1.3",
    "electron-config": "^0.2.1",
    "textract": "^2.1.1"
  }
}
